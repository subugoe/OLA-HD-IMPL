server {
  listen 80;
  server_name _;

  location /api/ {
    proxy_pass http://141.5.98.232:8080/;
    include /etc/nginx/conf.d/proxy_params;
  }

  location / {
      root  /usr/share/nginx/user-gui/html;
      try_files $uri $uri/ /index.html;
  }

  error_page 404 /index.html;
}